namespace Misc
{
    public enum GameState
    {
        Waiting,
        Playing,
        Paused,
        Win,
        Lose,
    }

    public enum PlayerStates
    {
        NormalMode,
        MagnetMode
    }
    
}